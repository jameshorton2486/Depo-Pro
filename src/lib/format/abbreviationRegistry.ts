import registryData from "../../../Canonical Standards Folder/abbreviation_registry.json";
import type { AbbreviationRegistry } from "./types";

export const abbreviationRegistry = registryData as AbbreviationRegistry;
