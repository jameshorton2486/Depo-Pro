// App.tsx is unused — the editor mounts directly via main.tsx → mountEditor().
// Kept as a shim to avoid module-not-found errors from any tooling that imports it.
export default function App() {
  return null;
}
