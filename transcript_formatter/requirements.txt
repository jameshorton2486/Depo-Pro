# Depo-Pro Tools — Python dependencies
# Install with: pip install -r requirements.txt

# UI framework
customtkinter>=5.2.0

# Anthropic AI
anthropic>=0.25.0

# Deepgram transcription
deepgram-sdk>=6,<7

# HTTP client (pinned for deepgram-sdk compatibility)
httpx>=0.27.0

# Document handling
python-docx>=1.1.0
pdfplumber>=0.10.0

# Environment / config
python-dotenv>=1.0.1

# Windows-only COM automation (Word track changes)
pywin32; sys_platform == "win32"

# Testing
pytest>=8.0.0

# PDF output
reportlab>=4.0.0
