# Transcription benchmark corpus

Only synthetic or appropriately de-identified and access-controlled material may be committed here. Real client audio and certified depositions must remain outside Git and be selected at runtime.

The manifest defines stable scenario IDs and expected input paths. A corpus runner can resolve private paths in an authorized environment without copying legal content into the repository.
